# 全流程跑通清单（2026-09-17）

> 现状一句话：**JD 分析主链路已实测可信（3/3），简历优化与公司调研从未跑通过，且 resume-tailor 新版还没部署进 app**。
> 按本文档从 Phase 0 到 Phase 6 顺序执行，每步带预期结果和失败排查点。

---

## 一、已完成并经过验证的能力

### 稳定性与链路（有实测记录）
- App Server 全链路：握手/通知/审批/超时一系列修复（`98f1bf6` `c16f9a7` `13f096e` `4e0396d`），JD 分析实测 3/3 成功
- 同岗位重跑分析的 artifacts 唯一约束冲突已修（`4422335`）
- 正式包 React 双实例崩溃已修（`33442e7`），正式包已装 /Applications，update-app.sh 一键更新
- 启动清理孤儿 AI 任务（`14960b7`）、启动清理停用内置 Skill（`27f3fd6`）

### jd-analyst 集成（主链路，已实测）
- 深解档为默认；匹配档自动携带基础简历快照
- ScorecardView 打分卡展示；匹配语义定案（节点不合并、只用基础简历、快照 + resume_ref）
- 优化达线门控：决策带 < 慎投偏投时简历优化按钮锁定（jobs.$jobId.tsx:93）

### 六连修（0a56357，代码就位、部分未实测）
- 优化成功自动入库（tailored 简历 parent_resume_id 链，service.rs:578-596）
- 优化前置校验（无基础简历/无分析拦截）
- 公司调研提示词强制 web_search
- skill 的 schemas/、references/ 递归复制进 run 目录
- 岗位编辑限优化前 / 岗位删除全局可用

### 排版与编辑器
- CMS 排版编辑器：时间轴块级搭接（`746f2d7`）、标题正文分离、连续时间轴（`1ec0bed`）
- 模板收敛 7→3；Markdown 编辑页（CodeMirror + A4 实时预览）有入口跳转排版编辑器（edit.tsx:225）
- 渲染管线三个纯 TS 包有单元测试（template-engine / domain / markdown-resume）

### 系统面
- Skill 导入/删除（设置页，`526b5f9`）、分组展示（`9058e88`）
- 备份/恢复（.jsw-backup，settings.tsx:285）
- 沟通/投递渠道扩展中国主流平台（`e58441a`）

---

## 二、未测试/未打通的路径（风险从高到低）

### ⚠️ P0：resume-tailor 新版部署断档
- app 里现在跑的是 **9/11 旧版**（"简历定向改写器"，简单改写）；**9/17 新稿**（"简历定制施工"：claim 四态表、Before/After、投递锦囊、结构继承）还躺在 `/Users/kyriecai/Documents/resume_workspace/skills/resume-tailor/`，未进 bundle
- 两版结构不同：新版带 `references/`（rewrite-rules.md、output-schema.md）、无 `schemas/`；旧版相反
- 输出文件名靠 run prompt 兜底（service.rs:790 已写死 `tailored-resume.md` + `tailoring-report.json`），skill 正文不写路径也不会错
- **部署方式**：替换 `apps/desktop/src-tauri/resources/skills/resume-tailor/` → 重启（内置 skill 启动时同步到用户数据目录；同名内置不能靠设置页导入覆盖）→ 最好跑 update-app.sh 重打包
- 注意：内存里的"resume-tailored.md"是过时记忆，代码与旧 skill 均为 `tailored-resume.md`，无命名冲突

### P1：简历优化全链路 0 实测
前置校验 → run → 产物校验 → tailored 简历自动入库 → 报告落库，整条链只被代码审过，从未真实跑过

### P1.5：tailoring-report.json 无专用展示 UI
全前端无任何组件消费它（入库后只能通过通用 artifacts 查看或翻文件）。跑通时确认，大概率是个产品缺口

### P2：公司调研修复后未复验
9/15 实跑 2 成 1 败（败因已溯源：用户全局 ~/.codex/AGENTS.md 的 web-access 规则拦截联网，非应用 bug）；"强制 web_search"提示词修复（0a56357）落地后从未复验稳定性

### P2：导入的 Skill 不能被任务点名调用
run prompt 硬编码三个内置 skill 名（service.rs:37-39）。导入功能本身也未实测；即使导入成功，任务也不会点名用它（只能靠 agent 自发触发）

### P3：PDF 导出 / 备份恢复 / 正式包走查
- WKWebView 的坑（相对路径落 CWD、就绪标记防空白快照）修过，正式包场景未系统复验
- 备份→恢复的完整回路未测（恢复到全新数据目录）
- 排版编辑器开发期迭代多，正式包未走查

### 待定设计：A/B 编辑模式
排版编辑器内「方案 A 画布直编」vs「方案 B 源文件粘贴（右抽屉）」两种编辑形态的取舍，待用户验证后拍板保留哪种，不在本清单范围

---

## 三、跑通路径（按依赖顺序执行）

### Phase 0 · 预检（不花 token）
- [ ] 确认设置页 codex 路径识别正常、`account/read` 显示已登录（email/planType）
- [ ] Skills 列表能看到三个内置 skill（jd-analyst / company-researcher / resume-tailor）
- [ ] **部署 resume-tailor 新版**（见 P0），重启后到用户数据目录确认 SKILL.md 已是"简历定制施工"版：
      `~/Library/Application Support/com.jobdesk.workbench/workspace/.agents/skills/resume-tailor/SKILL.md`

### Phase 1 · 简历基建
- [ ] 导入或新建一份基础简历（Markdown）
- [ ] 编辑页：CodeMirror 编辑 + A4 实时预览正常联动
- [ ] 跳转排版编辑器：块点选/拖拽排序/时间轴渲染正常
- [ ] 导出 PDF，检查落盘路径与内容完整性（防空白快照回归）

### Phase 2 · JD 分析（已验链路，作回归）
- [ ] 新建岗位，粘贴 JD，跑深解档分析（默认）
- [ ] 验证：jd-analysis.json 入库、打分卡 ScorecardView 渲染、岗位状态机推进
- [ ] 匹配档重跑（携带基础简历），对照两档打分差异
- [ ] 同岗位第三次重跑（回归 4422335 唯一约束修复）

### Phase 3 · 公司调研（复验修复）
- [ ] 对同岗位跑公司调研（9/15 曾 2 成 1 败，本轮验证强制 web_search 修复后的稳定性）
- [ ] 验证：company-research.md 内容含真实调研结果（非编造）、company-sources.json 是合法 JSON 且有来源
- [ ] 失败重点看：是否又走了全局 web-access 流程而非 web_search（rollout 日志 ~/.codex/sessions/年/月/日/）

### Phase 4 · 简历优化施工（首测，核心验证对象）
- [ ] 达线岗位：概览页"简历优化"按钮可点 → 触发优化 run
- [ ] 未达线岗位（或改 JD 重析制造低分）：按钮锁定 + 提示文案正确
- [ ] 无基础简历时触发：前置校验拦截并给出可读错误
- [ ] 优化成功：岗位简历 Tab 自动出现 tailored 简历（parent 指向基础简历）
- [ ] 检查新版 skill 的产出质量：claim 四态表、Before/After 变更、投递锦囊是否按新稿执行
- [ ] 优化后：岗位编辑入口被锁（限优化前）
- [ ] tailoring-report.json 已入库；确认目前没有展示界面（记录为产品缺口）

### Phase 5 · 投递管理闭环
- [ ] 录入沟通记录（选渠道）、创建投递、登记面试轮次
- [ ] 岗位状态机随事件正常流转（7 状态）
- [ ] Dashboard 指标卡数字与实际数据一致

### Phase 6 · 系统面与边缘路径
- [ ] 创建备份 → 恢复（最好恢复到全新数据目录，验证完整回路）
- [ ] 设置页导入一个测试 skill 目录 → 出现 → 删除（顺带体会"不能被点名调用"的现状）
- [ ] 删除一个已有关联数据（run/artifacts/简历）的岗位，确认全局删除不炸
- [ ] 模板库浏览 + 模板资产编辑

---

## 排查锚点（任何一步失败先看这里）
1. **AI 记录 Tab**：run 状态与错误信息
2. **run 工作目录**：`~/Library/Application Support/com.jobdesk.workbench/workspace/ai-runs/<run-id>/`（inputs/outputs 是否齐全）
3. **quarantine 目录**：两阶段提交失败的产物会被移入这里
4. **app-server 日志**：`$TMPDIR/jsw-logs/app-server-*.log`
